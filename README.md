# GLPI + Chatwoot Helpdesk Stack

Отказоустойчивая система управления заявками на базе Docker Compose.

## 🏗️ Архитектура

- **GLPI** - система управления IT-инфраструктурой и заявками
- **Chatwoot** - омниканальная платформа для работы с клиентами
- **MariaDB** - база данных для GLPI
- **PostgreSQL** - база данных для Chatwoot
- **Redis** - кэш и очередь задач для Chatwoot
- **Nginx Proxy Manager** (внешний) - SSL и проксирование

## ✨ Возможности

✅ **Отказоустойчивость:**
- Named volumes для сохранения данных при перезапуске
- Health checks для автоматического мониторинга
- Resource limits для предотвращения перегрузки
- Restart policies для автовосстановления

✅ **Безопасность:**
- Пароли в `.env` файле (не в Git)
- Redis с аутентификацией
- Изолированные сети

✅ **Функциональность:**
- Установка плагинов GLPI (volume `glpi_plugins`)
- Marketplace GLPI (volume `glpi_marketplace`)
- Автоматические бэкапы
- Готовность к интеграции с Telegram ботом

## 📋 Требования

- Docker Engine 20.10+
- Docker Compose v2+
- 4GB RAM минимум
- 20GB свободного места

## 🚀 Быстрый старт

### 1. Подготовка

```bash
# Клонировать репозиторий
cd /opt/helpdesk-stack

# Создать .env из шаблона
cp .env.example .env

# Редактировать .env (обязательно!)
nano .env
```

**Сгенерировать секреты:**
```bash
# Для CHATWOOT_SECRET_KEY_BASE
openssl rand -hex 64

# Для паролей
openssl rand -base64 24
```

### 2. Инициализация

```bash
# Сделать скрипты исполняемыми
chmod +x *.sh

# Запустить инициализацию
./init.sh
```

### 3. Установка

```bash
# Автоматическая установка
./install.sh

# Или вручную:
docker compose up -d glpi_db chatwoot_db chatwoot_redis
sleep 40
docker compose run --rm chatwoot bundle exec rails db:chatwoot_prepare
docker compose up -d
```

### 4. Настройка NPM (Nginx Proxy Manager)

**GLPI:**
- Domain: `glpi2.yapomogu.com`
- Forward Hostname: `v2_glpi`
- Forward Port: `80`
- WebSocket Support: ❌ OFF

**Chatwoot:**
- Domain: `chat2.yapomogu.com`
- Forward Hostname: `v2_chatwoot`
- Forward Port: `3000`
- WebSocket Support: ✅ ON (обязательно!)

### 5. Первый вход

**GLPI** (`glpi2.yapomogu.com`):
1. Откроется мастер установки
2. Выберите язык: Русский
3. Настройки БД:
   - Сервер: `glpi_db`
   - База: `glpi` (из .env)
   - Пользователь: `glpi` (из .env)
   - Пароль: (из .env)
4. После установки удалите installer:
   ```bash
   docker exec v2_glpi rm /var/www/html/install/install.php
   ```

**Chatwoot** (`chat2.yapomogu.com`):
1. Создайте Super Admin аккаунт
2. Настройте первый Inbox

## 🔧 Управление

### Статус системы

```bash
./status.sh
# или
docker compose ps
docker compose logs -f
```

### Остановка/Запуск

```bash
# Остановить
docker compose stop

# Запустить
docker compose start

# Перезапустить
docker compose restart

# Полная остановка с удалением контейнеров
docker compose down
```

### Logs

```bash
# Все логи
docker compose logs -f

# Конкретный сервис
docker compose logs -f glpi
docker compose logs -f chatwoot
docker compose logs -f glpi_db
```

## 💾 Бэкапы

### Создание бэкапа

```bash
./backup.sh
```

Создаст архив в `./backups/backup_YYYY-MM-DD_HH-MM.tar.gz`

### Автоматические бэкапы (cron)

```bash
# Редактировать crontab
crontab -e

# Добавить строку (бэкап каждую ночь в 3:00)
0 3 * * * /opt/helpdesk-stack/backup.sh >> /var/log/helpdesk-backup.log 2>&1
```

### Восстановление

```bash
# Показать доступные бэкапы
./restore.sh

# Восстановить из бэкапа
./restore.sh backup_2024-01-15_03-00.tar.gz
```

⚠️ **ВНИМАНИЕ:** Восстановление ПЕРЕЗАПИШЕТ все текущие данные!

## 📦 Volumes

Данные хранятся в Docker named volumes:

### GLPI:
- `glpi_files` - загруженные файлы
- `glpi_plugins` - установленные плагины
- `glpi_config` - конфигурация
- `glpi_marketplace` - плагины из Marketplace
- `glpi_db_data` - база данных MariaDB

### Chatwoot:
- `chatwoot_storage` - файлы чатов (аватары, вложения)
- `chatwoot_db_data` - база данных PostgreSQL
- `chatwoot_redis_data` - данные Redis

### Управление volumes:

```bash
# Список всех volumes
docker volume ls | grep -E "(glpi|chatwoot)"

# Размер volumes
docker system df -v | grep -E "(glpi|chatwoot)"

# Резервное копирование volume
docker run --rm -v glpi_files:/data -v $(pwd):/backup alpine tar czf /backup/glpi_files.tar.gz -C /data .

# Восстановление volume
docker run --rm -v glpi_files:/data -v $(pwd):/backup alpine tar xzf /backup/glpi_files.tar.gz -C /data
```

## 🔌 Установка плагинов GLPI

### Через веб-интерфейс (Marketplace):
1. GLPI → Setup → Plugins → Marketplace
2. Выберите плагин → Install

### Вручную:
```bash
# Скачать плагин в volume
docker exec v2_glpi wget -P /var/www/html/plugins https://example.com/plugin.tar.gz

# Распаковать
docker exec v2_glpi tar -xzf /var/www/html/plugins/plugin.tar.gz -C /var/www/html/plugins

# Установить права
docker exec v2_glpi chown -R www-data:www-data /var/www/html/plugins/plugin_name

# Активировать в веб-интерфейсе
```

## 🔗 Интеграция Chatwoot ↔ GLPI

### Текущий статус:
- ✅ Сетевое взаимодействие настроено (сеть `backend`)
- ⏳ API интеграция - готовится

### Планируется:
1. API Bridge микросервис для связи
2. Автоматическое создание тикетов GLPI из диалогов Chatwoot
3. Синхронизация статусов
4. Интеграция с Telegram ботом

## 🤖 Telegram бот (в разработке)

Структура подготовлена для добавления Telegram бота:
- Сетевая конфигурация готова
- API эндпоинты Chatwoot доступны
- Переменные окружения можно добавить в `.env`

## 📊 Мониторинг

### Health checks:

```bash
# Проверка здоровья всех сервисов
docker ps --format "table {{.Names}}\t{{.Status}}"
```

Все сервисы имеют health checks:
- GLPI: проверка HTTP /status.php
- Chatwoot: проверка API endpoint
- MariaDB: проверка подключения
- PostgreSQL: pg_isready
- Redis: redis-cli ping

## ⚙️ Resource Limits

Лимиты можно настроить в `.env`:

```env
GLPI_MEMORY_LIMIT=2g
GLPI_CPU_LIMIT=2.0
CHATWOOT_MEMORY_LIMIT=2g
CHATWOOT_CPU_LIMIT=2.0
DB_MEMORY_LIMIT=1g
DB_CPU_LIMIT=1.0
```

## 🔧 Troubleshooting

### Контейнер не запускается:
```bash
docker compose logs <service_name>
docker inspect <container_name>
```

### База данных не инициализируется:
```bash
# Проверить логи
docker compose logs glpi_db
docker compose logs chatwoot_db

# Пересоздать volume (УДАЛИТ ДАННЫЕ!)
docker compose down -v
docker volume rm glpi_db_data
docker compose up -d
```

### Chatwoot не подключается к БД:
```bash
# Проверить пароли в .env
cat .env | grep CHATWOOT_DB

# Проверить переменные в контейнере
docker exec chatwoot env | grep POSTGRES
```

### GLPI: "Installer не найден":
```bash
# Если нужно переустановить GLPI
docker exec v2_glpi touch /var/www/html/install/install.php
```

### Нет места на диске:
```bash
# Очистить неиспользуемые образы
docker system prune -a

# Удалить старые бэкапы
find ./backups -name "backup_*.tar.gz" -mtime +30 -delete
```

## 📝 Структура проекта

```
helpdesk-stack-npm/
├── docker-compose.yml      # Основная конфигурация
├── .env                    # Секреты (НЕ в Git!)
├── .env.example            # Шаблон .env
├── .gitignore              # Защита секретов
├── init.sh                 # Инициализация
├── install.sh              # Установка
├── backup.sh               # Создание бэкапа
├── restore.sh              # Восстановление
├── status.sh               # Проверка статуса
├── backups/                # Архивы бэкапов
└── README.md               # Эта документация
```

## 🛡️ Безопасность

- ✅ Пароли в `.env` (не в Git)
- ✅ Redis с паролем
- ✅ Изолированные сети
- ✅ Resource limits
- ⚠️ Рекомендуется: настроить firewall на хосте
- ⚠️ Рекомендуется: шифровать бэкапы (GPG)

## � Документация

### Для администраторов
- **[README.md](README.md)** - Общий обзор и быстрый старт
- **[DEPLOY.md](DEPLOY.md)** - Пошаговая инструкция развертывания на сервере
- **[CHEATSHEET.md](CHEATSHEET.md)** - Быстрая справка команд
- **[MIGRATION.md](MIGRATION.md)** - Миграция со старой версии

### Для разработчиков и ИИ
- **[TECHNICAL.md](TECHNICAL.md)** - Полная техническая документация
- **[AI_CONTEXT.md](AI_CONTEXT.md)** - Быстрый старт для ИИ-ассистентов
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений проекта

### Структура файлов
```
helpdesk-stack-npm/
├── docker-compose.yml      # Главная конфигурация
├── .env                    # Секреты (НЕ в Git)
├── .env.example            # Шаблон секретов
├── *.sh                    # Скрипты управления
└── *.md                    # Документация
```

## �📞 Поддержка

Логи системы:
```bash
./status.sh
docker compose ps
docker compose logs
```

## 🔄 Обновление

```bash
# Создать бэкап перед обновлением!
./backup.sh

# Остановить сервисы
docker compose down

# Обновить образы
docker compose pull

# Запустить
docker compose up -d

# Проверить логи
docker compose logs -f
```

## 📄 Лицензия

Этот проект использует:
- GLPI (GPL)
- Chatwoot (MIT)
